import { ScoreCountDirective } from './score-count.directive';

describe('ScoreCountDirective', () => {
  it('should create an instance', () => {
    const directive = new ScoreCountDirective();
    expect(directive).toBeTruthy();
  });
});
